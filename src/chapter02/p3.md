# a.

O(n)

---

# b.

NAIVE-POLYNOMIAL(A, n, x)
    p = 0
    for k = 0 to n
        pow = 1
        for j = 1 to k
            pow = pow * x
        p = p + A[i] * pow
        
The running time is O(n^2), because of the nested loop.

---

# c.


