# 1-1
[15, 13, 9, 5, 12, 8, 7, 4, 0, 6, 2, 1]
we max-heapfiy on [1, 13, 9, 5, 12, 8, 7, 4, 0, 6,2 ]

# 1-3
MIN-HEAP-MINIMUM
    if (A.heap_size < 1)
        error "underflow"
    return A[1]

MIN-HEAP-EXTRACT-MIN(A)
    min = MIN_HEAP_MINIMUM(A)
    A[1] = A[A.heap_size]
    A.heap_size -= 1
    MIN-HEAPIFY(A, 1)
    return min

MIN_HEAP_DECREASE_KEY(A, x, k)
    if (k > x)
        error "new key is bigger than current"

    x.key = k
    find index where x is
    while i > 1 and A[PARENT(i)].key > A[i].key
        swap A[i], A[PARENT(i)]
        i = PARENT(i)

Running time is lgn
