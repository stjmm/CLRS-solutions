# 1-1
[27, 17, 3, 16, 13, 10, 1, 5, 7, 12, 4, 8, 9, 0]
[27, 17, 10, 16, 13, 3, 1, 5, 7, 12, 4, 8, 9, 0]
[27, 17, 10, 16, 13, 9, 1, 5, 7, 12, 4, 8, 3, 0]

# 1-2

# 1-3
MIN-HEAPIFY(A, i)
    left = LEFT(i)
    right = RIGHT(i)

    if left <= A.heapsize and A[left] < A[i]
        smallest = left
    else
        smallest = i

    if right <= A.heapsize and A[right] < A[smallest]
        smallest = right

    if smallest != i
        swap(A[smallest], A[i])
        MAX-HEAPIFY(A, smallest)

Running time same.

# 1-4
Nothing is swapped. Recursive call doesnt happen.

# 1-5
A[n/2 + 1: n] are leafs.
Will do nothing since they are leaf nodes.

# 1-6
MAX-HEAPIFY-ITERATIVE(A, i)
    while true
        left = LEFT(i)
        right = RIGHT(i)

        if left <= A.heapsize and A[left] > A[i]
            largest = left
        else
            largest = i
        if right <= A.heapsize and A[right] > A[largest]
            largest = right

        if largest == i
            return

        swap(A[largest], A[i])
        i = largest
