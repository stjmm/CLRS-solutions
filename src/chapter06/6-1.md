# 1-1
Max: When every level from 0 to h is full. 1 + 2 + 3 + ... + 2^h = 2^h+1 - 1
Min: Levels 0 to h-1 full, h at least one node. 2^h

# 1-2
n = 2^m -1 + k, k are additional leaves at the bottom. To get to bottom we must move 2^m times. m = lgn

# 1-3
A[Parent(root)] >= A[i], A[Parent(root)] >= A[i+1], A[i], A[i+1] are greater that its children

# 1-4
At any bottom leaf.

# 1-6
N0.
