# 1-1
For every n > n0 f(n), g(n) >=0. BEcause nonnegative asymptotically.
f(n) <= max(f(n), g(n))
g(n) <= max(f(n), g(n))
(f(n) + g(n)) / 2 <= max(f(n), g(n))
max(f(n), g(n)) <= f(n) + g(n)
Combining:
0 <= (f(n) + g(n))/2 <= max(f(n), g(n)) <= f(n) + g(n)
Which is theta(f(n) + g(n))

# 1-2
Because O(n^2) is a upper bound and A coudl also be O(n). We need a tight bound.

# 1-3
2^(n+1) = 2^n * 2 <= c2^n, true when c >= 2
2^n * 2^n <= c2^n ?, there is no c and n0 such that 0 <= 2^2n = 4^n <= c2^n for all n >= n0
