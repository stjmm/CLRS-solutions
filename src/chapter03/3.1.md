# 1-1
When n is NOT divisible by 3.
So k = floor(n/3)
Now:
- Put the largest k elements into first k positions
- These elements will end up in last k positions
- Between those regiosn are at least k positions

So $k^2 = \lflorr{n/3^2}
$\floor{n/3^2} >= (n/3 - 1)^2 = n^2/9 - 2n/3 + 1$
Therfore Ω(n^2)  holds.

# 1-2
We assume n is a multiple of 3. k = n/3
We have three regions A[1:k], A[k + 1:2k], A[2k+1: n]
Consider first n/3 iterations fo the outer loop. During this at elast 2k/3 elements remain unsorted.
The inner loop must inspect every element in remaining eleemnts. THerfore it will execute (n - 1)(2k/3) so Ω(n^2)
