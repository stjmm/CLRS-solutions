# 1-1
We can just pad matrices with 0 so n is power of 2. We can also fallabck to nromal for loops when n is not power of 2.

# 1-2
kn x n * n x kn results in a kn x kn matrix
n kn * kn n results in a n x n matrix

n x n matrix wsill be asymptotically faster.

# 1-3

Increases memory alolocation but time is still T(n) = theta(n^3)

# 1-4

MATRIX_ADD_RECURSIVE(A, B, C, n)
    // Divide A, B, C intro 4 submatrices
    // A11, A12, A21, A22
    // B11, B12, B21, B22
    // C11, C12, C21, C22

    if n == 1
        C[1][1] = A[1][1] + B[1][1]

    MATRIX_ADD_RECURSIVE(A11, B11, C11, n / 2)
    MATRIX_ADD_RECURSIVE(A12, B12, C12, n / 2)
    MATRIX_ADD_RECURSIVE(A21, B21, C21, n / 2)
    MATRIX_ADD_RECURSIVE(A22, B22, C22, n / 2)

    T(n) = 4T(n/2) + theta(1)
    T(n) = theta(n^2)
